<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="DayTrack.Views.TrackerLogPage">
    
    <ContentPage.Content>
        <StackLayout>
            <DatePicker Date="{Binding DateToLog}" />
            <Button Text="LOG" Command="{Binding LogDayCommand}" />
            <ListView ItemsSource="{Binding AllDays}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell Text="{Binding Date, StringFormat='{0:M/d/yyyy}'}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
